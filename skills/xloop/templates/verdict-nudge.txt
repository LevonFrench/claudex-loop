Your preserved prior output did not satisfy the protocol contract. Re-read it at the findings path and correct its complete contents into the required schema. Preserve valid findings verbatim and drop every sentence that is not part of the schema.

Your final message is stored verbatim as the output path: it must contain only the artifact, with no preamble, tooling discussion, or code fence. Nobody is present, so never ask for approval, permission, or clarification; if you cannot write files, that is expected, and your final message is still the artifact. Under .loop nothing but the output path may change: STATE, PROTOCOL, PLAN, REQUEST, REVIEW-LOG, ASSUMPTIONS, QUESTIONS, and every packet evidence file are immutable, and any edit to them is restored and costs the round.

Finding IDs are exactly F<round>.<index> or B<round>.<index> and severity is blocking|major|minor. Every finding needs a concrete Scenario line; a finding without one is void. REVISE requires at least one blocking finding with a concrete Scenario line. APPROVE means zero findings and zero finding-shaped lines of any kind, and a bare [F5] or a header missing its severity or reference invalidates either verdict. The last non-blank line must be exactly VERDICT: APPROVE, VERDICT: REVISE, RESULT: PASS, or RESULT: FAIL, matching the artifact you were asked to produce.

Round: {{round}}
Protocol: {{protocol_path}}
Findings: {{findings_path}}
Output: {{output_path}}
