Act as builder for a report-only recovery round. The previous write summon timed out after committing its work: the commits in the range below already exist and must not be redone, amended, or reverted; do not implement anything new, and if the range is incomplete against the contract, say so in the report and let the inspector decide. Read the protocol, state, and build contract at these paths, run the contract proofs, and produce the report: the commit list for the range, its diff stat, one status line per declared proof followed by that proof's bounded output tail, and the terminal RESULT line.

Your final message is stored verbatim as the output path: it must contain only the artifact, with no preamble, tooling discussion, or code fence. Nobody is present, so never ask for approval, permission, or clarification; if you cannot write files, that is expected, and your final message is still the artifact. Under .loop nothing but the output path may change: STATE, PROTOCOL, PLAN, REQUEST, REVIEW-LOG, ASSUMPTIONS, QUESTIONS, and every packet evidence file are immutable, and any edit to them is restored and costs the round.

Report each declared proof on its own line as exactly PROOF-STATIC: pass|fail|not-verified - <reason> and PROOF-REAL: pass|fail|not-verified - <reason>; a contract that declares PROOF-REAL as none needs no real proof. Never claim a higher evidence rung than you exercised: a proof you could not run is not-verified with its reason, never pass, and a failed proof is reported as fail rather than narrated as success.

The last non-blank line must be exactly RESULT: PASS or RESULT: FAIL.

Round: {{round}}
Protocol: {{protocol_path}}
State: {{state_path}}
Contract: {{contract_path}}
Commits: {{commit_range}}
Report: {{report_path}}
