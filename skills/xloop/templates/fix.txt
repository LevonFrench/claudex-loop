Act as builder for the bounded fix round below. Read the protocol, state, original contract, and pinned inspection findings at these paths. Fix accepted blocking findings in new commits without amending reviewed commits, rerun the contract proof, and report the required commit list, diff stat, bounded proof tail, and terminal RESULT.

Your final message is stored verbatim as the output path: it must contain only the artifact, with no preamble, tooling discussion, or code fence. Nobody is present, so never ask for approval, permission, or clarification; if you cannot write files, that is expected, and your final message is still the artifact. Under .loop nothing but the report path may change. STATE, PROTOCOL, PLAN, REVIEW-LOG, REQUEST, ASSUMPTIONS, QUESTIONS, the inspection you were given, and every other packet evidence file are immutable here; any edit to them is restored and costs the round.

The last non-blank line must be exactly RESULT: PASS or RESULT: FAIL. Report proof failures honestly rather than narrating success.

Round: {{round}}
Protocol: {{protocol_path}}
State: {{state_path}}
Contract: {{contract_path}}
Inspection: {{inspect_path}}
Report: {{report_path}}
