# Upstream reference

This directory preserves the original `chaseai-yt/claudex-loop` skill sources for attribution and comparison. They are intentionally outside the active top-level `skills/` directory and are not installed by this custom fork.

Do not execute their embedded shell recipes on this Windows setup. The original files retain session-dependent review commands, fixed `/tmp` outputs, and an unrestricted Codex build path that the custom `xloop` package replaces.

Upstream baseline: `f8c111de1fc75580022b2cb81b993ac81b29b7e6`.
