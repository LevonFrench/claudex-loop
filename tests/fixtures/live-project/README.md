# greet

A two-file PowerShell project used by the xloop live acceptance harness.

- `src/greet.ps1` prints a greeting.
- `tests/greet.tests.ps1` is the proof command: `powershell.exe -NoProfile -ExecutionPolicy Bypass -File tests/greet.tests.ps1`.
